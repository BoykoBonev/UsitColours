@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Styles.Render("~/Content/admin/css")

<div class="container body-content">
    <div class="page-content">
        <div class="row" style="padding-top:20px;">
            <div class="col-md-2">
                <div class="sidebar content-box" style="display: block;">
                    <ul class="nav">
                        <li>
                            @Ajax.ActionLink(
                           "Flight History", "FlightHistory", "Profile", new AjaxOptions()
                           {
                               HttpMethod = "GET",
                               AllowCache = false,
                               InsertionMode = InsertionMode.Replace,
                               UpdateTargetId = "profile-content"
                           },
                           new
                           {
                               @class = "glyphicon glyphicon-plus"
                           })
                        </li>
                        <li>
                            @Ajax.ActionLink(
                           "Upcomming Flights", "UpcommingFlights", "Profile", new AjaxOptions()
                           {
                               HttpMethod = "GET",
                               AllowCache = false,
                               InsertionMode = InsertionMode.Replace,
                               UpdateTargetId = "profile-content"
                           },
                           new
                           {
                               @class = "glyphicon glyphicon-plus"
                           })
                        </li>
                        <li>
                            @Ajax.ActionLink(
                           "Job History", "JobHistory", "Profile", new AjaxOptions()
                           {
                               HttpMethod = "GET",
                               AllowCache = false,
                               InsertionMode = InsertionMode.Replace,
                               UpdateTargetId = "profile-content"
                           },
                           new
                           {
                               @class = "glyphicon glyphicon-plus"
                           })
                        </li>
                        <li>
                            @Ajax.ActionLink(
                           "Upcomming Flights", "UpcommingFlights", "Profile", new AjaxOptions()
                           {
                               HttpMethod = "GET",
                               AllowCache = false,
                               InsertionMode = InsertionMode.Replace,
                               UpdateTargetId = "profile-content"
                           },
                           new
                           {
                               @class = "glyphicon glyphicon-plus"
                           })
                        </li>
                        
                    </ul>
                </div>
            </div>
            <div class="col-md-10">
                <div class="content container-fluid">
                    <div id="profile-content">
                        <h1>Profile page</h1>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@Scripts.Render("~/bundles/unobtrusive")


